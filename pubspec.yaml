name: compta_app
description: Application de facturation et caisse pour petites entreprises (Congo)
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  uuid: ^4.3.3
  intl: ^0.19.0
  pdf: ^3.10.7
  printing: ^5.11.1
  share_plus: ^7.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
